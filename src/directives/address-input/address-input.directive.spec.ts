import { AddressInputDirective } from './address-input.directive';

describe('AddressInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AddressInputDirective();
    expect(directive).toBeTruthy();
  });
});
