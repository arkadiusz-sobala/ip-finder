import { AddressInfoPipePipe } from './address-info-pipe.pipe';

describe('AddressInfoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AddressInfoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
